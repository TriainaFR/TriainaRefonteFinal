# Dossier Images

C'est ici que vous pouvez stocker toutes les images du site pour qu'elles soient bien organisées.

1. Déposez vos fichiers ici (ex: `photo.jpg`).
2. Dans le code (`constants.ts`), utilisez le chemin : `/images/photo.jpg`.
